
const mainMenu = document.getElementById("mainMenu");
const miniHeight = document.querySelector(".miniMenu").offsetHeight;

window.addEventListener("scroll", () => {
    if (window.scrollTo > miniHeight) {
        mainMenu.classList.add()
    }
})